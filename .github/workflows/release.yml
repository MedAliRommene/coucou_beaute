name: 🏷️ Release & Tag

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to release (e.g., v1.0.0)'
        required: true
        type: string

jobs:
  # ===========================================
  # JOB 1: CRÉATION DE LA RELEASE
  # ===========================================
  create-release:
    name: 🏷️ Create Release
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        
    - name: 🏷️ Get version
      id: version
      run: |
        if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
          echo "version=${{ github.event.inputs.version }}" >> $GITHUB_OUTPUT
        else
          echo "version=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
        fi
        
    - name: 📝 Generate changelog
      id: changelog
      run: |
        # Générer un changelog basique
        echo "## 🚀 Release ${{ steps.version.outputs.version }}" > CHANGELOG.md
        echo "" >> CHANGELOG.md
        echo "### ✨ Nouvelles fonctionnalités" >> CHANGELOG.md
        echo "- Améliorations de l'interface utilisateur" >> CHANGELOG.md
        echo "- Optimisations des performances" >> CHANGELOG.md
        echo "" >> CHANGELOG.md
        echo "### 🐛 Corrections de bugs" >> CHANGELOG.md
        echo "- Corrections diverses" >> CHANGELOG.md
        echo "" >> CHANGELOG.md
        echo "### 🔧 Améliorations techniques" >> CHANGELOG.md
        echo "- Mise à jour des dépendances" >> CHANGELOG.md
        echo "- Amélioration de la sécurité" >> CHANGELOG.md
        
    - name: 🏷️ Create Release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ steps.version.outputs.version }}
        release_name: Release ${{ steps.version.outputs.version }}
        body_path: CHANGELOG.md
        draft: false
        prerelease: false

  # ===========================================
  # JOB 2: DÉPLOIEMENT AUTOMATIQUE
  # ===========================================
  deploy-release:
    name: 🚀 Deploy Release
    needs: create-release
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
      
    - name: 🔐 Setup SSH
      uses: webfactory/ssh-agent@v0.7.0
      with:
        ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}
        
    - name: 🚀 Deploy release to server
      run: |
        ssh -o StrictHostKeyChecking=no ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} '
          cd /opt/coucou_beaute
          
          # Récupérer la version spécifique
          git fetch --tags
          git checkout ${{ github.ref_name }}
          
          # Sauvegarder la configuration
          if [ -f backend/.env ]; then
            cp backend/.env backend/.env.backup.$(date +%s)
          fi
          
          # Redéployer
          docker compose -f docker-compose.prod.yml down
          docker compose -f docker-compose.prod.yml up -d
          
          # Vérifier le déploiement
          sleep 30
          if curl -f http://localhost:80 > /dev/null 2>&1; then
            echo "✅ Release déployée avec succès !"
          else
            echo "❌ Problème de déploiement de la release"
            exit 1
          fi
        '

  # ===========================================
  # JOB 3: NOTIFICATION DE RELEASE
  # ===========================================
  notify-release:
    name: 📢 Notify Release
    needs: [create-release, deploy-release]
    runs-on: ubuntu-latest
    if: always()
    
    steps:
    - name: 📧 Send release notification
      run: |
        if [ "${{ needs.deploy-release.result }}" == "success" ]; then
          echo "🎉 Release ${{ github.ref_name }} déployée avec succès !"
          # Ici vous pouvez ajouter des notifications (Slack, Discord, etc.)
        else
          echo "❌ Échec du déploiement de la release ${{ github.ref_name }}"
          # Ici vous pouvez ajouter des notifications d'erreur
        fi
